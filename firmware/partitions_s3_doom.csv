# ESP32-S3-Touch-AMOLED-1.75C partition table with Doom WAD support
# 16MB flash total. tools/espdisp.py stages this as partitions.csv only in
# the s3-175 target's private build copy; c6 and s3-185 keep standard layouts.
#
# Layout:
#   0x000000 - 0x008FFF  Bootloader (36KB, managed by esptool)
#   0x009000 - 0x00DFFF  NVS (20KB) - WiFi creds, device name, orientation, brightness
#   0x00E000 - 0x00FFFF  OTA data (8KB)
#   0x010000 - 0x5FFFFF  app0 (6,225,920 bytes)
#   0x600000 - 0xBEFFFF  app1 (6,225,920 bytes)
#   0xBF0000 - 0xBFEFFF  reserved alignment gap
#   0xBFF000 - 0xFFFFFF  doom_wad (4,198,400 bytes)
#
# The canonical 4,196,020-byte Doom shareware v1.9 WAD fits with 2,380
# bytes to spare. Every offset and size is erase-sector aligned, and the
# final partition ends exactly at the 16MiB flash boundary.
#
# The WAD partition uses a custom type/subtype (0x42/0x06) following the
# convention from espressif/esp32-doom. It is memory-mapped only when the
# one-shot Doom boot starts, so reads are zero-copy from flash cache.
#
# Name,    Type, SubType, Offset,   Size,     Flags
nvs,       data, nvs,     0x9000,   0x5000,
otadata,   data, ota,     0xE000,   0x2000,
app0,      app,  ota_0,   0x10000,  0x5F0000,
app1,      app,  ota_1,   0x600000, 0x5F0000,
doom_wad,  0x42, 0x06,    0xBFF000, 0x401000,
