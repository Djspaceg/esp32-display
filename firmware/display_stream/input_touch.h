// Touch servicing: polls the controller, wakes a dimmed panel (consuming
// that press), shows the quick info bar on a plain tap, and forwards
// completed gestures to the sender.
#pragma once

void serviceTouch();
