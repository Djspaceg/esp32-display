// The CFG* serial configuration surface (USB CDC, 115200): WiFi credentials,
// device name, orientation, power, board override, LED diagnostics, OTA
// password, tuning knobs, and CFGSHOW.
#pragma once

void handleSerialConfig();
